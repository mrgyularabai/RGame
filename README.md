# RGame – Lightweight 2D Game Engine for Python

RGame is a simple Python Game engine, which uses `tkinter` for Graphics.

## How to Install?
The easiest way to install at the moment is to download the RGameLib.py file.
The PyPI repository is currently under maintenance.

## How to Run?
Run the MainScript.py file using python.